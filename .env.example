# GraphRAG 知识库系统 - 环境变量配置文件
# 请复制此文件为 .env 并根据实际需求修改配置参数
# 文件说明: 定义各个基础服务的配置参数，包括数据库连接、认证信息等

# =============================================================================
# PostgreSQL 数据库配置
# 用于存储文档元数据、文本块和系统配置信息
# =============================================================================
POSTGRES_DB=graphrag
POSTGRES_USER=graphrag
POSTGRES_PASSWORD=graphrag123
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# =============================================================================
# Neo4j 图数据库配置  
# 用于存储实体关系图和知识图谱
# =============================================================================
NEO4J_USER=neo4j
NEO4J_PASSWORD=neo4j123
NEO4J_DATABASE=graphrag
NEO4J_HOST=localhost
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687

# =============================================================================
# Redis 缓存配置
# 用于缓存查询结果、会话管理和临时数据存储
# =============================================================================
REDIS_PASSWORD=redis123
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# =============================================================================
# MinIO 对象存储配置
# 用于存储原始文档、处理后的文件和媒体资源
# =============================================================================
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_HOST=localhost
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_BUCKET_NAME=graphrag-documents

# =============================================================================
# Weaviate 向量数据库配置
# 用于语义搜索、向量存储和相似性检索
# =============================================================================
WEAVIATE_HOST=localhost
WEAVIATE_PORT=8080
WEAVIATE_SCHEME=http

# =============================================================================
# MinerU 文档解析服务配置
# 用于 PDF 解析、OCR 识别和文档结构化处理
# =============================================================================
MINERU_HOST=localhost
MINERU_PORT=8501

# =============================================================================
# 应用程序配置
# GraphRAG 系统的核心配置参数
# =============================================================================
# 应用环境 (development/production/testing)
APP_ENV=development

# API 服务端口
API_PORT=8000

# 日志级别 (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO

# JWT 密钥 (生产环境请使用强密码)
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# =============================================================================
# AI 模型配置
# 用于实体抽取、关系识别和文本处理的 AI 模型配置
# =============================================================================
# OpenAI API 配置 (可选)
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-3.5-turbo

# Hugging Face 配置 (可选)
HUGGINGFACE_API_KEY=your-huggingface-api-key

# 本地模型配置
LOCAL_MODEL_PATH=/app/models

# =============================================================================
# 文档处理配置
# 控制文档解析、分块和处理的参数
# =============================================================================
# 文档分块大小 (字符数)
CHUNK_SIZE=1000

# 分块重叠大小 (字符数)  
CHUNK_OVERLAP=200

# 支持的文档类型
SUPPORTED_FILE_TYPES=pdf,txt,md,html,docx

# 最大文件大小 (MB)
MAX_FILE_SIZE=100

# =============================================================================
# 监控和健康检查配置
# =============================================================================
# 健康检查间隔 (秒)
HEALTH_CHECK_INTERVAL=30

# 服务超时时间 (秒)
SERVICE_TIMEOUT=60